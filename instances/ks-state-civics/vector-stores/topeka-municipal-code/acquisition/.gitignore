# Raw publisher bytes are a large reproducible corpus: they belong in the cell
# object store with a checksummed lock, not in Git. The manifests, checkpoint
# and reports beside them are the committed proof.
*/raw/
