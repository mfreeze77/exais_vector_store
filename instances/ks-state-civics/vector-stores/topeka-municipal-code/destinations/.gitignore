# release-selection.jsonl is a regenerable working file (3.5MB): it is whatever
# topeka-release-selection.py computed from the current manifests. The committed
# proof is releases/proofs/release-selection.json, which carries the counts and
# the bucket accounting.
release-selection.jsonl
