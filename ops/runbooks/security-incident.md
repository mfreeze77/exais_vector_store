# Security Incident Runbook

Freeze deployments, rotate keys, export audit logs, identify impacted docs/chunks, disable affected indexes, rebuild if ACL metadata was wrong, notify per policy, add regression tests.
